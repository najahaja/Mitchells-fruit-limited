# Mitchell's Fruit Limited — frontend environment variables
# Copy to frontend/.env before npm run dev or npm run build.
# Rebuild the frontend after changing VITE_* values.

# Backend API base URL (must include /api suffix)
VITE_BASE_URL=http://localhost:8000/api
